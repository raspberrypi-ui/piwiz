This is a wizard to make initial settings to a Raspberry Pi on first boot.

To build, run ./autogen.sh, then ./configure and make.
Use sudo make install to install data files.

To run again, run `sudo piwiz` from the command line
